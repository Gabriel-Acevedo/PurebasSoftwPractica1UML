2) Paquete Damas.Utils:

@startuml

title Damas MVC(Paquete Damas.Utils) - Class Diagram

package damas.utils{

    class WithConsoleView{
        #console: Console
        #WithConsoleView()
    }
    class ClosedInterval{
        -min: int
        -max: int
        +ClosedInterval(int,int)
        +includes(int): boolean
    }
    class Console{
        -bufferedReader: BufferedReader
        +Console()
        +write(char): void
        +write(String): void
        +readInt(String): int
        +readChar(String): char
        +readString(String): String
        +writeln(int): void
        +writeln(String): void
        +writeln(): void
        -writeError(String): void
    }
    class YesNoDialog{
        -{static} AFIRMATIVE: char
        -{static} NEGATIVE: char
        -{static} QUESTION: String
        -{static} MESSAGE: String
        +YesNoDialog()
        +read(String): boolean
        -{static}isNegative(char): boolean
        -{static}isAfirmative(char): boolean
    }
}
package java.io{
    class BufferedReader{
    }
}

WithConsoleView *-down-> Console
Console *-down-> BufferedReader
WithConsoleView <|-down- YesNoDialog

@enduml