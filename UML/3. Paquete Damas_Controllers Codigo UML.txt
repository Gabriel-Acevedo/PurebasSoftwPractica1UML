3) Paquete Damas.controller:

@startuml
title Damas MVC(Paquete Dama.controllers) - Class Diagram

package damas.controller {
    class Controller{
        #game: Game
        ~Controller(Game)
    }
    class StartController{
        +StartController(Game)
    }
    class MoveController{
        +MoveController(Game)
        +MoveToken(TokenNumber,positionX,positionY): void
        +getPositionX(TokenNumber): int
        +getPositionY(TokenNumber): int
        +isKill(positionX,positionY): boolean
        +isQueen(positionX,positionY): boolean
        +isWinner(): boolean
        +isLoose(): boolean
    }
    class ResumeController{
        +ResumeController(Game)
        +resume(boolean): void
    }
}    

package damas.models {
    class Game{
    }
}

Controller *-down-> Game
Controller <|-down- StartController
Controller <|-down- MoveController
Controller <|-down- ResumeController

@enduml